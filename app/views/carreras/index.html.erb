<h1>Carreras </h1>


<%= link_to 'Agregar Carrera', new_carrera_path, :class => "btn btn-info" %>



<Table class="table">
  <tr>
    <thead class="thead-dark">
    <th> Nombre Carrera </th>
    <th> Codigo Carrera </th>
    <th> Estado </th>
    
    <th> Opciones </th>
  </thead>
  </tr>

  <% @todocarrera.each do |t| %>
  <tr>
  <td><%= t.nombrecarrera %></td>
  <td><%= t.codigo %></td>
  <td class="text-success">
	<%if t.estado ==true %>
	Activo 
 <%else%> 
	Inactivo
<%end%>
  </td>
  <td>

  <%= link_to 'Mostrar', carrera_url(t), class: "btn btn-outline-success  btn-sm" %>
     
      
  <%= link_to 'Editar', edit_carrera_url(t), class:"btn btn-outline-warning  btn-sm" %>

    
	<% unless t.estado.nil? %>
	<% if t.estado == true %>
			
	  <%= link_to 'Cambiar estado', edit_carrera_url(t), class: "btn btn-danger btn-sm",
                        method: :delete, data: {confirm: '¿Estas seguro?'} %>
    <% end %>
	  <% end %>
</td>
</tr>
<%end%>
</table>




